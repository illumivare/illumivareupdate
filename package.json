{
  "name": "illumivare",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port=3000 --host=0.0.0.0",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "rm -rf dist",
    "lint": "echo 'Pure HTML5 static site — no linting required.'"
  },
  "dependencies": {
    "vite": "^6.2.3"
  }
}
